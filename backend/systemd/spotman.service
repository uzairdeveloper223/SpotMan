[Unit]
Description=SpotMan API Backend
After=network.target

[Service]
User=root
WorkingDirectory=/opt/spotman/backend
Environment="PATH=/opt/spotman/backend/.venv/bin"
ExecStart=/opt/spotman/backend/.venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
